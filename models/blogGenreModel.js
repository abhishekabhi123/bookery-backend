const mongoose = require("mongoose");

const blogGenreSchema = new mongoose.Schema(
  {
    title: {
      type: String,
      required: true,
      unique: true,
      index: true,
    },
  },
  {
    timestamps: true,
  }
);

module.exports = mongoose.model("BGenre", blogGenreSchema);
